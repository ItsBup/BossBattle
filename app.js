console.log('hello there! ~Obi-Wan Kenobi')

function updateThings(){
    array.forEach(element => {
        let thingEl = document.getElementByID(element.name)
        console.log('💀', thingEl)
        let statsElm = thingElm.querySelector('.stats')
    });
}